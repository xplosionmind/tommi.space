---
title: Zibaldone feed
subtitle: Il flusso degli ultimi articoli scritti su Zibaldone, il blog di tommi.space
altUrl: https://tommi.space/zibaldone/
permalink: /zibaldone.xml
lang: it
layout: feed.xml.liquid
---
{% assign zib = collections.zibaldone | sort: 'date' | reverse %}
{% for entry in zib limit: 10 %}
	<item>
		<title>{{ entry.data.title }}</title>
		<link>{{ site.url }}{{ entry.url }}</link>
		<description><![CDATA[{{ entry.content | replace: '"/', '"https://tommi.space/' | replace: '\'/', '\'https://tommi.space/' }}]]></description>
		<author>{{ site.author.email }} ({{ site.author.name }})</author>
		{% for tag in tags %}
			<category>{{ tag }}</category>
		{% endfor %}
		<guid>{{ site.url }}{{ entry.url }}</guid>
		<pubDate>{{ entry.date | date: '%a, %-d %b %Y %H:%m:%S %z' }}</pubDate>
	</item>
{% endfor %}

